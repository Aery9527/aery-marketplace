# v0.12.1

來源：v0.12.0

## 快速導覽

- [概覽](#概覽)
- [升級注意事項](#升級注意事項)
- [重構](#重構)

---

## 概覽

`v0.12.1` 將 `claude-code-bridge` skill 更名為 `claude-code`，讓 Codex 端的觸發識別碼與 plugin manifest 既有的 `Claude Code` 顯示名稱一致。目錄結構、frontmatter `name`、bundle catalog 與所有內部路徑參照同步更新，並重新產生 `codex-plugins/aery-claude-code` 封裝與同步後的 marketplace 清單。

[回到頂端](#快速導覽)

---

## 升級注意事項

- 已安裝 `aery-claude-code` bundle 的 Codex 使用者，這次更新後 skill 觸發識別碼從 `claude-code-bridge` 改為 `claude-code`；沿用舊識別碼的自訂 prompt 或腳本需要一併更新。
- 顯示名稱（`Claude Code Bridge`）、`SKILL.md` 標題與內文中作為概念描述的「the bridge」維持不變，這次只改識別碼與路徑參照。

[回到頂端](#快速導覽)

---

## 重構

- 將 `claude-code-bridge` skill 目錄、frontmatter `name`、bundle catalog（`.agents/plugins/codex-only-bundles.json`）、`AGENTS.md`／`AGENTS_zhTW.md` 與 `docs/UPSTREAM-PARITY.md` 的路徑參照統一更名為 `claude-code`，並同步更新 codex-plugin 的 `default_prompt` 與內部 temp 目錄前綴，避免識別碼殘留（`refactor(claude-code): rename skill from claude-code-bridge`）。

[回到頂端](#快速導覽)
