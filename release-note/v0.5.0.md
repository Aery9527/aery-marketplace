# v0.5.0

來源：v0.4.0

## 功能

- **arch-rules**：擴展觸發覆蓋率，要求 agent 在任何程式碼修改、介面定義或命名決策時主動載入；新增第 10 節「程式碼修改協定」（Orient → Contract → Blast Radius → Minimal Surface → Verify）及第 11 節「介面與型別設計」（命名、大小、契約、演進四面向指引）；同步更新繁體中文版本與 Codex 同步檔 (`feat(arch-rules): expand trigger coverage and add code-modification protocol with interface design guide`)

## 修正

- **windows-script**：修正繁體中文版 description 中英夾雜問題，並將縮排從 3 格統一為 2 格 (`fix(windows-script): correct mixed-language description and indentation in zhTW`)

## 文件

- **image-to-html**：為 pixel-alignment-playbook 新增 Quick Navigation 錨點與章節分隔線，方便在長文件中快速跳轉 (`docs(image-to-html): add Quick Navigation anchors to pixel-alignment-playbook`)
- 對齊 write-md skill 的人類文件撰寫規範 (`docs: align human-facing markdown with write-md conventions`)
