{
  "skill_name": "plan-rules",
  "evals": [
    {
      "id": 1,
      "prompt": "我想要為 jackpot payout flow 寫一份實作計畫（在 game-slot-gp-app 中）。功能概述：當 jackpot 金額累積超過閾值，且玩家觸發 hit 條件時，系統要扣減 jackpot collection 的金額並記錄中獎歷史。請幫我建立計畫文件。",
      "expected_output": "建立 game-slot-gp-app/docs/sys/jackpot-payout-flow/jackpot-payout-flow-100000-plan.md（單一 submodule 範疇，放在 submodule 底下），包含模組架構、資料模型（Go struct）、至少三個行為規格（觸發判斷、金額扣減、歷史記錄），每個行為都有具體 Input/Output/Edge cases，非目標小節明確列出不含 frontend payout 顯示。"
    },
    {
      "id": 2,
      "prompt": "準備一份 multi-currency support 的設計文件，給開發團隊看的那種。背景是現在只支援 CNY，未來要支援 USD/THB/VND，各 Brand 對應不同幣別。",
      "expected_output": "建立 docs/sys/multi-currency-support/multi-currency-support-100000-design.md（跨 submodule 範疇，放在 root 底下），全繁體中文，包含背景問題陳述、範疇（含/不含）、解決方案概覽、關鍵設計決策（為何用 Brand 層級而非 User 層級），不包含 Go struct 定義。"
    },
    {
      "id": 3,
      "prompt": "設計一個新遊戲註冊 API，讓 mgt-app 可以透過 gRPC 新增遊戲定義到 gp platform。需要同時寫 design 跟 plan 兩份文件。",
      "expected_output": "建立 docs/sys/game-registration-api/game-registration-api-100000-design.md 和 docs/sys/game-registration-api/game-registration-api-100000-plan.md（跨 submodule 範疇，放在 root 底下）。design 為 SA 層級說明為何需要此 API；plan 為 SD 層級含 proto 介面定義、handler 行為 SBE、非目標列出不含遊戲刪除功能。"
    }
  ]
}
